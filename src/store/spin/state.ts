export interface SpinState {
  maskMain: boolean;
  maskView: boolean;
  splash: boolean;
}

const state: SpinState = {
  maskMain: false,
  maskView: false,
  splash: false,
};

export default state;
